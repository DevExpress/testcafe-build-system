name: Test

on: 
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      
      - id: detect
        uses: ./actions/detect-build-scripts

      - uses: actions/github-script
        with:
          script: |
            const json = (${{ steps.detect.outputs.scripts }});

            console.log(json[0])