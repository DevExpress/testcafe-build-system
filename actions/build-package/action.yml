name: Build package

runs:
  using: "composite"
  steps:
    - id: detect_build_scripts
      uses: ./actions/detect-build-scripts

    - run: "echo ${{ steps.detect_build_scripts.outputs.scripts[0] }}""
