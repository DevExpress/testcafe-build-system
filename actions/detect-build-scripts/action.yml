name: "Detect build scripts"

outputs:
  scripts:
    value: ""

runs:
  using: "composite"
  steps: 
    - uses: actions/github-script@v3
      with: 
        script: |
          console.log(process.cwd());
          core.setOutput("scripts", Object.keys(require('./package.json').scripts).filter(script => script.startsWith('build')))
